<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('./partials/head.ejs') %>
</head>
<body>
    <h1>Index. Iconic Moments in MLB History</h1>
    <div class="container">
        <ul>
            <% for (let i = 0; i < baseballs.length; i++) { %>
            <li>
                <div class="images">
                <img src="<%=baseballs[i].img%>">
                </div>
                <a href="/baseballs/<%=baseballs[i].id%>"><br/>
                    <%=baseballs[i].title%>
                </a><br/>
                <%=baseballs[i].date%><br/>
                <%=baseballs[i].description%>
                <form action="/baseballs/<%=baseballs[i].id%>?_method=DELETE" method="POST">
                    <input type="submit" value="DELETE" />
                </form>
                <form action="/baseballs/<%=baseballs[i].id%>/edit"><input type="submit" value="Edit" />
                </form>
                </li>
            <% } %>
        </ul>
    </div>
</body>
</html>