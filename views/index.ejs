<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title></title>
</head>
<body>
    <h1>Index. Iconic Moments in MLB History</h1>
    <div class="container">
        <ul>
            <% for (let i = 0; i < baseballs.length; i++) { %>
            <li>
                <a href="/baseballs/<%=baseballs[i].id%>">
                <%=baseballs[i].title%>
                </a>
                <img src="<%=baseballs[i].img%>">
                <form action="/baseballs/<%=baseballs[i].id%>?_method=DELETE" method="POST">
                    <input type="submit" value="DELETE" />
                </form>
                <a href="/baseballs/<%=baseballs[i].id%>/edit">Edit</a>
                </li>
            <% } %>
        </ul>
    </div>
</body>
</html>